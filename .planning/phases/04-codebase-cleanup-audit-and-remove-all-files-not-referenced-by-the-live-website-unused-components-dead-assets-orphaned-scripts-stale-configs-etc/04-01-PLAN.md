---
phase: 04-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/CurtleAboutUs.jsx
  - src/app/components/ChiliIcon.tsx
  - src/app/components/DishGallery.tsx
  - src/app/components/AboutUs.tsx
  - src/app/components/CustomSelect.jsx
  - src/components/MatrixText.tsx
  - src/components/customHook/useResize.jsx
  - src/app/(main)/home/ChatWindow.tsx
  - src/app/(main)/home/QualifyForm.tsx
  - src/style/AboutUs.css
  - git_hub_production
  - test-flex.html
  - .eslintrc.js
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "All 9 orphaned source components are deleted from the repo"
    - "No empty directories remain where deleted files lived"
    - "Legacy ESLint config is removed and modern flat config still works"
    - "Root-level scratch files are gone"
  artifacts:
    - path: "src/app/components/"
      provides: "Only active components remain"
      absent:
        - "CurtleAboutUs.jsx"
        - "ChiliIcon.tsx"
        - "DishGallery.tsx"
        - "AboutUs.tsx"
        - "CustomSelect.jsx"
    - path: "src/components/"
      provides: "Only active components remain"
      absent:
        - "MatrixText.tsx"
        - "customHook/"
    - path: "src/style/"
      provides: "Directory removed (was only AboutUs.css)"
      absent:
        - "AboutUs.css"
  key_links:
    - from: "eslint.config.mjs"
      to: ".eslintrc.js"
      via: "Only eslint.config.mjs should exist"
      pattern: "eslint\\.config\\.mjs"
---

<objective>
Delete all orphaned source components, empty parent directories, legacy config, and root-level scratch files.

Purpose: Remove dead code that creates confusion and bloats the repository. These 13 files have zero importers and serve no purpose in the live site.
Output: A cleaner src/ tree and project root with only active files remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-codebase-cleanup-audit-and-remove-all-files-not-referenced-by-the-live-website-unused-components-dead-assets-orphaned-scripts-stale-configs-etc/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all 9 orphaned source components and empty parent directories</name>
  <files>
    src/app/components/CurtleAboutUs.jsx
    src/app/components/ChiliIcon.tsx
    src/app/components/DishGallery.tsx
    src/app/components/AboutUs.tsx
    src/app/components/CustomSelect.jsx
    src/components/MatrixText.tsx
    src/components/customHook/useResize.jsx
    src/app/(main)/home/ChatWindow.tsx
    src/app/(main)/home/QualifyForm.tsx
    src/style/AboutUs.css
  </files>
  <read_first>
    src/app/components/Menu.tsx (confirm it does NOT import any of the files being deleted)
    src/app/components/MobileMenuCard.tsx (confirm it does NOT import DishGallery or CustomSelect)
    src/app/components/DesktopMenuCarousel.tsx (confirm it does NOT import DishGallery)
  </read_first>
  <action>
    Safety check first: grep the entire src/ directory for imports of each file being deleted. Confirm zero importers for all 9 components. The expected result is zero matches for each — if ANY match is found, do NOT delete that file and report the unexpected import.

    Grep patterns to confirm zero importers:
    - "CurtleAboutUs" — expect 0 matches outside its own file
    - "ChiliIcon" — expect 0 matches outside its own file
    - "DishGallery" — expect 0 matches outside its own file
    - "MatrixText" — expect 0 matches outside its own file
    - "useResize" — expect 0 matches outside its own file
    - "ChatWindow" — expect 0 matches outside its own file
    - "QualifyForm" — expect 0 matches outside its own file
    - "AboutUs" — expect 0 matches outside AboutUs.tsx and AboutUs.css
    - "CustomSelect" — expect 0 matches outside its own file

    After confirming zero importers, delete the following files per D-01 through D-09:
    1. rm src/app/components/CurtleAboutUs.jsx (per D-01)
    2. rm src/app/components/ChiliIcon.tsx (per D-02)
    3. rm src/app/components/DishGallery.tsx (per D-03)
    4. rm src/components/MatrixText.tsx (per D-04)
    5. rm src/components/customHook/useResize.jsx (per D-05)
    6. rm src/app/(main)/home/ChatWindow.tsx (per D-06)
    7. rm src/app/(main)/home/QualifyForm.tsx (per D-07)
    8. rm src/app/components/AboutUs.tsx (per D-08)
    9. rm src/style/AboutUs.css (per D-08, paired with AboutUs.tsx)
    10. rm src/app/components/CustomSelect.jsx (per D-09)

    After file deletions, remove empty parent directories:
    - rmdir src/components/customHook/ (was only useResize.jsx)
    - rmdir src/style/ (was only AboutUs.css)

    Do NOT touch:
    - src/app/components/Menu.tsx
    - src/app/components/MobileMenuCard.tsx
    - src/app/components/DesktopMenuCarousel.tsx
    - public/images/Week1/nonveg1/ (per D-10)
    - update_menu.js (per D-16)
    - eslint.config.mjs (per D-17, this is the KEEP file)
  </action>
  <verify>
    <automated>cd "/Users/SaadHazari/Library/Mobile Documents/com~apple~CloudDocs/1.Projects/Dormer's/IT/Website/Dormers-Production" && test ! -f src/app/components/CurtleAboutUs.jsx && test ! -f src/app/components/ChiliIcon.tsx && test ! -f src/app/components/DishGallery.tsx && test ! -f src/components/MatrixText.tsx && test ! -f src/components/customHook/useResize.jsx && test ! -f "src/app/(main)/home/ChatWindow.tsx" && test ! -f "src/app/(main)/home/QualifyForm.tsx" && test ! -f src/app/components/AboutUs.tsx && test ! -f src/style/AboutUs.css && test ! -f src/app/components/CustomSelect.jsx && test ! -d src/components/customHook && test ! -d src/style && test -f src/app/components/Menu.tsx && test -f src/app/components/MobileMenuCard.tsx && test -f src/app/components/DesktopMenuCarousel.tsx && echo "PASS: All 10 orphaned files deleted, 2 empty dirs removed, 3 active files preserved"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/components/CurtleAboutUs.jsx does not exist
    - src/app/components/ChiliIcon.tsx does not exist
    - src/app/components/DishGallery.tsx does not exist
    - src/components/MatrixText.tsx does not exist
    - src/components/customHook/ directory does not exist
    - src/app/(main)/home/ChatWindow.tsx does not exist
    - src/app/(main)/home/QualifyForm.tsx does not exist
    - src/app/components/AboutUs.tsx does not exist
    - src/style/ directory does not exist
    - src/app/components/CustomSelect.jsx does not exist
    - src/app/components/Menu.tsx still exists (not deleted)
    - src/app/components/MobileMenuCard.tsx still exists (not deleted)
    - src/app/components/DesktopMenuCarousel.tsx still exists (not deleted)
  </acceptance_criteria>
  <done>All 9 orphaned source components (10 files) are deleted. The customHook/ and style/ directories are removed. All active components are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Delete root-level artifacts and legacy ESLint config</name>
  <files>
    git_hub_production
    test-flex.html
    .eslintrc.js
  </files>
  <read_first>
    .eslintrc.js (confirm it is the legacy config, not the active one)
    eslint.config.mjs (confirm this is the active flat config that will remain)
  </read_first>
  <action>
    Delete three root-level files per D-14, D-15, D-17:
    1. rm git_hub_production (per D-14 — 730-line old source code dump)
    2. rm test-flex.html (per D-15 — scratch flexbox test file)
    3. rm .eslintrc.js (per D-17 — legacy ESLint config; eslint.config.mjs is the active flat config)

    After deleting .eslintrc.js, run a quick ESLint check to confirm eslint.config.mjs still works:
    npx eslint --max-warnings=999 src/app/components/Menu.tsx

    The lint command should complete without "config not found" errors. Lint warnings are acceptable; the point is confirming ESLint resolves the flat config file correctly.

    Do NOT touch:
    - eslint.config.mjs (per D-17, this is the KEEP file)
    - update_menu.js (per D-16)
  </action>
  <verify>
    <automated>cd "/Users/SaadHazari/Library/Mobile Documents/com~apple~CloudDocs/1.Projects/Dormer's/IT/Website/Dormers-Production" && test ! -f git_hub_production && test ! -f test-flex.html && test ! -f .eslintrc.js && test -f eslint.config.mjs && test -f update_menu.js && echo "PASS: 3 root artifacts deleted, eslint.config.mjs and update_menu.js preserved"</automated>
  </verify>
  <acceptance_criteria>
    - git_hub_production does not exist
    - test-flex.html does not exist
    - .eslintrc.js does not exist
    - eslint.config.mjs still exists (not deleted)
    - update_menu.js still exists (not deleted)
    - Running `npx eslint src/app/components/Menu.tsx` does not produce "config not found" or "no configuration" errors
  </acceptance_criteria>
  <done>All 3 root-level artifacts are deleted. ESLint still resolves the flat config file correctly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `test ! -f src/app/components/CurtleAboutUs.jsx` (and all 9 other orphaned files)
2. `test ! -d src/components/customHook && test ! -d src/style` (empty dirs gone)
3. `test ! -f git_hub_production && test ! -f test-flex.html && test ! -f .eslintrc.js`
4. `test -f eslint.config.mjs && test -f update_menu.js` (safe files preserved)
5. `test -f src/app/components/Menu.tsx` (active components preserved)
</verification>

<success_criteria>
- 13 files deleted (10 orphaned source + 3 root artifacts)
- 2 empty directories removed (customHook/, style/)
- eslint.config.mjs resolves correctly after .eslintrc.js removal
- All active components (Menu.tsx, MobileMenuCard.tsx, DesktopMenuCarousel.tsx) untouched
- update_menu.js untouched
</success_criteria>

<output>
After completion, create `.planning/phases/04-codebase-cleanup-audit-and-remove-all-files-not-referenced-by-the-live-website-unused-components-dead-assets-orphaned-scripts-stale-configs-etc/04-01-SUMMARY.md`
</output>
