---
phase: 01-foundations-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/components/Menu.tsx
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "Dish images are served as WebP or AVIF — no full-size JPG/PNG is downloaded on mobile viewports"
    - "The single <Image> rendering dish photos carries sizes=(max-width: 1024px) 140px, 336px"
    - "next build completes with zero image-sizing warnings in stdout"
    - "All 48 dishes in MENU_DATA have a spiceLevel field with value 2 (integer, not string, not undefined)"
    - "All 48 dishes in MENU_DATA have an allergens field with value [] (empty array, not null, not undefined)"
    - "TypeScript compilation succeeds — no new type errors introduced"
  artifacts:
    - path: "next.config.ts"
      provides: "Image optimization enabled — unoptimized flag absent"
      contains: "remotePatterns"
      must_not_contain: "unoptimized"
    - path: "src/app/components/Menu.tsx"
      provides: "Dish interface, typed MENU_DATA array, sized Image component"
      exports: []
      contains_all:
        - "type SpiceLevel = 1 | 2 | 3"
        - "type AllergenType ="
        - "type Week ="
        - "interface Dish"
        - "image: string | StaticImageData"
        - "spiceLevel: SpiceLevel"
        - "allergens: AllergenType[]"
        - "const MENU_DATA: Dish[]"
        - 'sizes="(max-width: 1024px) 140px, 336px"'
  key_links:
    - from: "next.config.ts"
      to: "next/image optimization pipeline"
      via: "absence of unoptimized: true"
      pattern: "^(?!.*unoptimized)"
    - from: "src/app/components/Menu.tsx (Image tag)"
      to: "browser srcset selection"
      via: "sizes attribute on <Image fill>"
      pattern: 'sizes=.*1024px.*140px.*336px'
    - from: "src/app/components/Menu.tsx (MENU_DATA)"
      to: "TypeScript type safety"
      via: "const MENU_DATA: Dish[] annotation"
      pattern: "const MENU_DATA: Dish\[\]"
---

<objective>
Enable Next.js image optimization and extend the MENU_DATA type model with spice level and allergen fields.

Purpose: Phase 2 card gallery cards display spice and allergen data, and they depend on optimized images being fast. Both must be in place before gallery work begins.
Output: A `next.config.ts` with optimization enabled, and a `Menu.tsx` with a typed `Dish` interface covering `spiceLevel` and `allergens`, plus placeholder values on all 48 dish objects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-data/01-RESEARCH.md
</context>

<interfaces>
<!-- Current state of the two files being modified. Read before touching anything. -->

From next.config.ts (lines 1-16) — current state:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true, // Optional: disables built-in image optimization  <-- DELETE THIS LINE
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
};

export default nextConfig;
```

From Menu.tsx (lines 1-14) — current imports:
```typescript
"use client";

import { useState, useEffect, useRef } from "react";
import Image from "next/image";           // <-- must become: import Image, { StaticImageData } from "next/image";
import { Box, Modal } from "@mui/material";
import { useTheme } from "next-themes";
import CustomSelect from "@/app/components/CustomSelect";
import ChickenAfghani from '../../../public/images/Week1/nonveg1/chickenAfghan.png';
import DormersChicken from '../../../public/images/Week1/nonveg1/DormersChicken.png';
import PeriPeri from '../../../public/images/Week1/nonveg1/PeriPeri.png';
import Meatballs from '../../../public/images/Week1/nonveg1/MeatballsMashe.png';
import ChickenFried from '../../../public/images/Week1/nonveg1/ChickenFried.png';
import ChickenBiryani from '../../../public/images/Week1/nonveg1/ChickenBiryani.png';
```

From Menu.tsx (lines 15-36) — commented-out interface to replace:
```typescript
// interface Nutrient {
//   name: string;
//   amount: string;
//   percentage?: string;
// }

// interface Dish {
//   id: number;
//   name: string;
//   description: string;
//   image: string;         <-- wrong: must be string | StaticImageData
//   isVeg: boolean;
//   dayOfWeek: number;
//   nutrients: { ... };
// }
```

From Menu.tsx (lines 1427-1433) — Image tag to modify:
```tsx
<div className="relative w-35 h-[147px] rounded-2xl overflow-hidden bg-[#EEE9DA] lg:h-[300px] lg:w-[336px] md:rounded-[33px]">
  <Image
    src={currentDish.image}
    alt={currentDish.name}
    fill
    className="object-cover rounded-2xl"    // <-- add sizes above or below this line
  />
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enable image optimization and add sizes to the dish Image tag</name>
  <files>next.config.ts, src/app/components/Menu.tsx</files>

  <read_first>
    - next.config.ts (full file — 16 lines)
    - src/app/components/Menu.tsx lines 1425–1435 (Image tag context)
  </read_first>

  <action>
**File 1: next.config.ts**

Delete the line `unoptimized: true, // Optional: disables built-in image optimization` (line 6).
Keep everything else exactly as-is. The resulting file must be:

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
};

export default nextConfig;
```

Do NOT touch `remotePatterns` — it is correct and must stay.

**File 2: src/app/components/Menu.tsx — Image tag only**

Find the `<Image>` tag near line 1428 that has `src={currentDish.image}` and `fill`. Add the `sizes` prop immediately after `fill`:

Before:
```tsx
<Image
  src={currentDish.image}
  alt={currentDish.name}
  fill
  className="object-cover rounded-2xl"
/>
```

After:
```tsx
<Image
  src={currentDish.image}
  alt={currentDish.name}
  fill
  sizes="(max-width: 1024px) 140px, 336px"
  className="object-cover rounded-2xl"
/>
```

Do NOT add `sizes` to any other `<img>` tags in the file (the VegIcon.svg and NonVeg.svg references use plain `<img>` elements — leave those alone).

Rationale: The dish image container is `w-35` (140px) on mobile and `lg:w-[336px]` (336px) on desktop. The `lg:` Tailwind breakpoint is 1024px. Without `sizes`, the browser assumes 100vw and fetches a ~400px image on mobile instead of 140px.
  </action>

  <acceptance_criteria>
- `grep "unoptimized" next.config.ts` returns no output (line deleted)
- `grep "remotePatterns" next.config.ts` returns the remotePatterns block (not deleted)
- `grep 'sizes=' src/app/components/Menu.tsx` returns exactly one line containing `(max-width: 1024px) 140px, 336px`
- The sizes value is on the Image tag that also has `src={currentDish.image}` — confirmed by reading lines 1428–1434
  </acceptance_criteria>

  <verify>
    <automated>grep "unoptimized" next.config.ts; echo "exit: $?" && grep -c 'sizes="(max-width: 1024px) 140px, 336px"' src/app/components/Menu.tsx</automated>
  </verify>

  <done>
`next.config.ts` contains no `unoptimized` key. `Menu.tsx` has exactly one `sizes="(max-width: 1024px) 140px, 336px"` attribute on the dish `<Image>` tag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Uncomment and expand the Dish interface, type MENU_DATA, add spice and allergen placeholders to all 48 dishes</name>
  <files>src/app/components/Menu.tsx</files>

  <read_first>
    - src/app/components/Menu.tsx lines 1–36 (imports + commented interface block)
    - src/app/components/Menu.tsx lines 38–185 (first 6 dishes — ids 1–6 with static image imports)
    - src/app/components/Menu.tsx lines 185–600 (dishes ids 7–24 — spot-check structure)
    - src/app/components/Menu.tsx lines 600–1000 (dishes ids 25–48 — spot-check structure)
  </read_first>

  <action>
Make three targeted edits to `src/app/components/Menu.tsx`. Do NOT restructure the file, move MENU_DATA, or change anything outside the three edit zones below.

**Edit 1: Update the `next/image` import (line 4)**

Change:
```typescript
import Image from "next/image";
```
To:
```typescript
import Image, { StaticImageData } from "next/image";
```

**Edit 2: Replace the commented-out interface block (lines 15–36)**

Delete ALL lines from `// interface Nutrient {` through the closing `// }` of the `// interface Dish` block. Replace with the following uncommented type and interface definitions:

```typescript
type SpiceLevel = 1 | 2 | 3;
type Week = 'week1' | 'week2' | 'week3' | 'week4';
type AllergenType = 'gluten' | 'dairy' | 'nuts' | 'eggs' | 'soy' | 'shellfish';

interface MicroNutrient {
  name: string;
  amount: string;
  percentage: string;
}

interface Dish {
  id: number;
  name: string;
  week: Week;
  description: string;
  image: string | StaticImageData;
  isVeg: boolean;
  dayOfWeek: number;
  spiceLevel: SpiceLevel;
  allergens: AllergenType[];
  nutrients: {
    calories: string;
    protein: string;
    carbs: string;
    fat: string;
    microNutrients: MicroNutrient[];
  };
}
```

**Edit 3: Add type annotation and placeholder fields**

3a. Change the array declaration from:
```typescript
const MENU_DATA = [
```
To:
```typescript
const MENU_DATA: Dish[] = [
```

3b. Add `spiceLevel: 2,` and `allergens: [],` to every dish object. There are exactly 48 dish objects. Add the two fields after the `isVeg` field in each object. The resulting shape for each dish is:

```typescript
{
  id: N,
  name: "...",
  week: "weekN",
  description: "...",
  image: ...,
  isVeg: true | false,
  dayOfWeek: N,
  spiceLevel: 2,       // DATA-01: placeholder, integer 1|2|3
  allergens: [],       // DATA-02: placeholder, empty = unknown/not yet classified
  nutrients: { ... },
},
```

Apply this to all 48 dishes — ids 1 through 48.

Type constraint notes:
- `spiceLevel: 2` is correct TypeScript for `SpiceLevel = 1 | 2 | 3`. Do NOT use `spiceLevel: "2"` (string).
- `allergens: []` is correct TypeScript for `AllergenType[]`. Do NOT use `allergens: null` or omit the field.
- The `week` values in existing dish objects (`"week1"`, `"week2"`, `"week3"`, `"week4"`) satisfy the new `Week` union type — no changes needed to existing `week` values.
- Ids 1–6 use `image: ChickenAfghani` etc. (StaticImageData). The `image: string | StaticImageData` type covers this — no change to those image fields.
  </action>

  <acceptance_criteria>
- `grep "StaticImageData" src/app/components/Menu.tsx` returns at least 2 lines (import line + interface field)
- `grep "type SpiceLevel" src/app/components/Menu.tsx` returns `type SpiceLevel = 1 | 2 | 3`
- `grep "type Week" src/app/components/Menu.tsx` returns `type Week = 'week1' | 'week2' | 'week3' | 'week4'`
- `grep "type AllergenType" src/app/components/Menu.tsx` returns the allergen union
- `grep "interface Dish" src/app/components/Menu.tsx` returns the uncommented interface declaration
- `grep "const MENU_DATA: Dish\[\]" src/app/components/Menu.tsx` returns exactly one match
- `grep -c "spiceLevel: 2" src/app/components/Menu.tsx` returns 48
- `grep -c "allergens: \[\]" src/app/components/Menu.tsx` returns 48
- No commented-out interface lines remain: `grep "// interface Dish" src/app/components/Menu.tsx` returns no output
  </acceptance_criteria>

  <verify>
    <automated>cd "/Users/SaadHazari/Library/Mobile Documents/com~apple~CloudDocs/1.Projects/Dormer's/IT/Website/Dormers-Production" && grep -c "spiceLevel: 2" src/app/components/Menu.tsx && grep -c "allergens: \[\]" src/app/components/Menu.tsx && grep "const MENU_DATA: Dish\[\]" src/app/components/Menu.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>
TypeScript compiles with no new errors. `grep -c "spiceLevel: 2"` returns 48. `grep -c "allergens: \[\]"` returns 48. The `Dish` interface is uncommented, typed, and includes `spiceLevel: SpiceLevel` and `allergens: AllergenType[]`. `MENU_DATA` is annotated as `Dish[]`.
  </done>
</task>

</tasks>

<verification>
Run the following after both tasks complete:

```bash
# 1. Confirm unoptimized is gone
grep "unoptimized" next.config.ts
# Expected: no output

# 2. Confirm sizes attribute present (should return 1)
grep -c 'sizes="(max-width: 1024px) 140px, 336px"' src/app/components/Menu.tsx

# 3. Confirm all 48 spiceLevel placeholders (should return 48)
grep -c "spiceLevel: 2" src/app/components/Menu.tsx

# 4. Confirm all 48 allergen placeholders (should return 48)
grep -c "allergens: \[\]" src/app/components/Menu.tsx

# 5. TypeScript — no new errors
npx tsc --noEmit

# 6. Build — no image warnings
npm run build 2>&1 | grep -i "image\|warn\|error"
```
</verification>

<success_criteria>
1. `next.config.ts` has no `unoptimized` key — Next.js image optimization is active.
2. The `<Image>` rendering dish photos has `sizes="(max-width: 1024px) 140px, 336px"` — mobile devices fetch 140px images, not 400px ones.
3. `npx tsc --noEmit` passes with no new errors.
4. `grep -c "spiceLevel: 2" src/app/components/Menu.tsx` returns 48.
5. `grep -c "allergens: \[\]" src/app/components/Menu.tsx` returns 48.
6. The `Dish` interface is active (not commented), typed with `SpiceLevel`, `Week`, `AllergenType`, and `image: string | StaticImageData`.
7. `const MENU_DATA: Dish[]` — the array carries a type annotation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-data/01-01-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
