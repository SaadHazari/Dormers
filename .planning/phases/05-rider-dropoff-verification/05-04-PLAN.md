---
phase: 05-rider-dropoff-verification
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified: []
autonomous: false
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05, VER-06, VER-07, VER-08, VER-09, VER-10, VER-11, VER-12, VER-13, ARC-03]

must_haves:
  truths:
    - "Full flow works: pickup confirm -> tap dorm -> camera opens -> take photo -> enter count -> submit -> verification result"
    - "Green tick animation shows on triple match"
    - "Camera works on mobile (375px)"
    - "TypeScript compiles clean"
    - "Lint passes"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete drop-off verification flow end-to-end: compile check, lint, and human visual verification at 375px mobile.

Purpose: Catch integration issues between the three prior plans (domain, API route, client UI) before moving to Phase 6 notification fanout.
Output: Owner approval of the drop-off flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rider-dropoff-verification/05-01-SUMMARY.md
@.planning/phases/05-rider-dropoff-verification/05-02-SUMMARY.md
@.planning/phases/05-rider-dropoff-verification/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile and lint verification</name>
  <files></files>
  <read_first>
    - src/app/ops/[token]/RiderClient.tsx (verify it exists with drop-off modal)
    - src/app/api/ops/verify-box-count/route.ts (verify it exists with maxDuration)
    - src/contexts/ops/domain/box-count-verify.ts (verify it exists)
    - src/contexts/ops/usecases/update-delivery-event.ts (verify it exists)
  </read_first>
  <action>
Run the following checks:

1. `npx tsc --noEmit` -- must pass with no errors in the Phase 5 files:
   - src/contexts/ops/domain/box-count-verify.ts
   - src/contexts/ops/usecases/update-delivery-event.ts
   - src/app/api/ops/verify-box-count/route.ts
   - src/app/ops/[token]/RiderClient.tsx
   - src/app/ops/[token]/actions.ts

2. `npm run lint` -- must pass with no new errors in the ops/ directory

3. Verify key integration points by reading the files:
   - RiderClient.tsx imports and calls `confirmDropoff` from `./actions`
   - RiderClient.tsx POSTs to `/api/ops/verify-box-count`
   - route.ts imports `verifyBoxCount` from `@/contexts/ops/domain/box-count-verify`
   - route.ts imports `updateDeliveryEvent` from `@/contexts/ops/usecases/update-delivery-event`
   - route.ts imports `notifyAdmin` from `@/infra/admin-alerts/notify`
   - route.ts has `export const maxDuration = 60`
   - box-count-verify.ts uses `google('gemini-2.5-flash')`

4. If any errors, fix them before proceeding to the visual checkpoint.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty false 2>&1 | grep -E "box-count-verify|update-delivery|verify-box-count|RiderClient|actions\.ts" | head -10; npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` produces zero errors for all Phase 5 files
    - `npm run lint` produces zero new errors in src/app/ops/ and src/contexts/ops/ and src/app/api/ops/
    - All import paths resolve correctly (no "Cannot find module" errors)
    - route.ts has `export const maxDuration = 60` (ARC-03 verified)
  </acceptance_criteria>
  <done>All Phase 5 files compile and lint clean, all imports resolve, maxDuration = 60 confirmed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of drop-off flow at 375px mobile</name>
  <files></files>
  <read_first>
    - src/app/ops/[token]/RiderClient.tsx (the UI being verified)
  </read_first>
  <action>
Present the drop-off verification flow to the owner for visual and functional approval. The owner needs to verify:

1. Pickup flow is unchanged (dorm buttons, confirm pickup, green confirmed state)
2. After pickup, tapping a dorm opens the camera modal
3. Camera captures a photo (getUserMedia or file input fallback)
4. Count input works with large tap target
5. Submit is disabled until photo + count > 0
6. Verification result shows correctly (green tick, retake, manual, escalation)
7. Modal closes cleanly with camera indicator off
8. Works at 375px iPhone SE width
  </action>
  <verify>
    <automated>echo "Checkpoint -- requires human approval"</automated>
  </verify>
  <acceptance_criteria>
    - Owner types "approved" after testing the drop-off flow
    - Or owner describes issues to fix
  </acceptance_criteria>
  <done>Owner has approved the drop-off verification flow visually and functionally</done>
</task>

</tasks>

<verification>
1. TypeScript compiles clean
2. Lint passes
3. Owner visually verifies the drop-off flow at 375px
4. Camera opens and captures photo
5. Submit disabled until photo + count (VER-12)
6. Verification result displays correctly
</verification>

<success_criteria>
- All files compile and lint clean
- Owner approves the drop-off verification flow visually
- Camera works on mobile
- The full pickup -> drop-off -> verify cycle functions end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-rider-dropoff-verification/05-04-SUMMARY.md`
</output>
