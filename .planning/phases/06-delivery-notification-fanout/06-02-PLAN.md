---
phase: 06-delivery-notification-fanout
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: false
requirements: [NOT-04, DB-06]

must_haves:
  truths:
    - "The dispatcher migration is applied to the live Supabase database"
    - "tpl_delivery_confirmed Vault secret exists with the approved Meta template name"
    - "TypeScript compiles without errors across the entire project"
    - "Lint passes with zero errors"
  artifacts: []
  key_links:
    - from: "dispatch_customer_notifications_tick"
      to: "vault.decrypted_secrets WHERE name = 'tpl_delivery_confirmed'"
      via: "Vault lookup"
      pattern: "tpl_delivery_confirmed"
---

<objective>
Apply the dispatcher migration to Supabase, insert the Vault secret, and verify the full project compiles cleanly.

Purpose: Plan 01 creates the code artifacts. This plan lands them in the live database and verifies no build regressions. The Vault secret insertion requires the user to have a Meta-approved template name first.

Output: Live dispatcher function with delivery_confirmed CASE, Vault secret in place, clean compile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-delivery-notification-fanout/06-RESEARCH.md
@.planning/phases/06-delivery-notification-fanout/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile and lint verification</name>
  <files></files>
  <read_first>
    src/contexts/ops/usecases/queue-delivery-confirmed-notifications.ts
    src/app/api/ops/verify-box-count/route.ts
  </read_first>
  <action>
Run the TypeScript compiler and linter to verify Plan 01's changes introduce no errors.

1. Run `npx tsc --noEmit` from project root. Must exit 0.
2. Run `npm run lint` from project root. Must exit 0 with no errors (warnings are acceptable).

If either fails, fix the issue in the files created/modified by Plan 01:
- `src/contexts/ops/usecases/queue-delivery-confirmed-notifications.ts`
- `src/app/api/ops/verify-box-count/route.ts`

Common issues to watch for:
- Missing type assertion on Supabase `.data` (cast to explicit array type like getDormCounts does)
- `no-unused-vars` on destructured variables (use `void` pattern if needed)
- Import path typos
  </action>
  <verify>
    <automated>cd /Users/SaadHazari/1Projects/developr/Dormers-Production && npx tsc --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - `npm run lint` exits with code 0 (no errors)
    - No type errors in `queue-delivery-confirmed-notifications.ts`
    - No type errors in `verify-box-count/route.ts`
  </acceptance_criteria>
  <done>Full project compiles and lints cleanly with the new fanout code</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Apply migration + insert Vault secret</name>
  <action>
The dispatcher migration and Vault secret must be applied to the live Supabase database (Ohio project `yjjayivwfqjfppawgyaz`).

**Sequence is critical (Pitfall 2 from RESEARCH):**

1. **Meta template** — Create a UTILITY template in Meta Business Manager:
   - Category: UTILITY (NOT marketing — Pitfall 3)
   - Language: English (`en`)
   - Header: text with `{{1}}` param (will receive first_name)
   - Body: static text, e.g. "Your meal just arrived at your dorm! Enjoy your dinner."
   - Wait for status to show "Active"

2. **Vault secret** — Once the template is approved and you know its exact name:
   ```sql
   SELECT vault.create_secret('<exact_meta_template_name>', 'tpl_delivery_confirmed', '');
   ```
   Run this via Supabase MCP against the Ohio project.

3. **Migration** — Apply the dispatcher migration:
   ```sql
   -- Copy the full contents of:
   -- supabase/migrations/20260616_customer_notifications_dispatcher_v7_delivery_confirmed.sql
   ```
   Run via Supabase MCP `apply_migration` against the Ohio project.

4. **Verify** — Confirm the migration landed:
   ```sql
   SELECT routine_name FROM information_schema.routines
   WHERE routine_name = 'dispatch_customer_notifications_tick'
   AND routine_schema = 'public';
   ```
   And confirm the Vault secret:
   ```sql
   SELECT name FROM vault.decrypted_secrets WHERE name = 'tpl_delivery_confirmed';
   ```
  </action>
  <how-to-verify>
    After applying migration + Vault secret, verify the end-to-end path:
    1. Check Vault: `SELECT name, decrypted_secret FROM vault.decrypted_secrets WHERE name = 'tpl_delivery_confirmed'` — should return the template name
    2. Check dispatcher function exists: `SELECT routine_name FROM information_schema.routines WHERE routine_name = 'dispatch_customer_notifications_tick'`
    3. Optionally: insert a test notification row and call the dispatcher to confirm it processes correctly (or wait for the next real delivery verification)
  </how-to-verify>
  <resume-signal>Type "migration applied" once Meta template is approved, Vault secret is inserted, and migration is applied</resume-signal>
  <acceptance_criteria>
    - Vault secret `tpl_delivery_confirmed` exists in live Supabase Ohio project
    - `dispatch_customer_notifications_tick` function is updated with the `delivery_confirmed` CASE branch
    - Meta template is in "Active" status in Business Manager
  </acceptance_criteria>
  <done>Live database has the dispatcher CASE branch and Vault secret. The full pipeline is operational: verified delivery -> fanout use-case -> customer_notifications row -> dispatcher tick -> Meta WhatsApp API -> customer receives message.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npm run lint` exits 0
3. Vault secret `tpl_delivery_confirmed` exists in live DB
4. Dispatcher function updated in live DB
5. No stuck `delivery_confirmed` rows in customer_notifications after first real delivery
</verification>

<success_criteria>
- Project compiles and lints with zero errors
- Live database dispatcher handles `delivery_confirmed` kind
- Vault secret points to an Active Meta UTILITY template
- End-to-end: verified delivery queues notifications that the dispatcher can send
</success_criteria>

<output>
After completion, create `.planning/phases/06-delivery-notification-fanout/06-02-SUMMARY.md`
</output>
