---
phase: 03-kitchen-display
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: []
autonomous: false
requirements: [KIT-07]

must_haves:
  truths:
    - "Page renders correctly at 375px with no horizontal overflow"
    - "All text is readable, all tap targets are reachable"
    - "Recipe modal works end-to-end: open, switch tabs, scroll, close"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the kitchen display at 375px mobile width. User confirms the dark UI, dish cards, count display, and recipe modal work correctly on a phone-sized screen.

Purpose: Kitchen staff use this on a phone propped up in the kitchen. If it doesn't work at 375px, nothing else matters. This checkpoint catches layout issues that TypeScript compilation cannot detect.

Output: User approval or a list of issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-kitchen-display/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and capture 375px screenshots</name>
  <files></files>
  <read_first>
    - src/app/kitchen/[token]/page.tsx (to find the route structure)
    - src/app/kitchen/[token]/KitchenClient.tsx (to understand what should render)
  </read_first>
  <action>
1. Start the Next.js dev server if not already running: `npm run dev`
2. Identify a valid kitchen token. Query the dev seed tokens:
   ```
   grep -r "kitchen" supabase/migrations/ | grep "INSERT"
   ```
   Or check the Phase 2 migration files for the seeded kitchen token string.
3. Use the headless browser recipe from memory (`/tmp/pw-runner` pattern with `playwright-core` + cached Chromium) to capture screenshots at 375px width:
   - Screenshot 1: The main kitchen page showing dish cards and count display
   - Screenshot 2: The recipe modal opened (Ingredients tab)
   - Screenshot 3: The recipe modal (Method tab)
   - Screenshot 4: The recipe modal (Notes tab)
4. Save screenshots to `/tmp/kitchen-verification/` for user review.
5. Also verify:
   - No horizontal scrollbar at 375px (document.documentElement.scrollWidth <= 375)
   - All text visible (no overflow: hidden cutting off content)
   - Count cards display correctly (both fit side-by-side at 375px)
  </action>
  <verify>
    <automated>ls /tmp/kitchen-verification/*.png 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - At least 2 screenshots captured at 375px width in /tmp/kitchen-verification/
    - Screenshots show dark background (#091825 navy)
    - Screenshots show dish cards with photos and names
    - Screenshots show count display (estimated or confirmed based on time of day)
  </acceptance_criteria>
  <done>Screenshots captured and ready for user review</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of kitchen display at 375px</name>
  <files></files>
  <action>
Present screenshots from /tmp/kitchen-verification/ to user and provide the dev URL for manual testing. User verifies the complete kitchen display page works correctly at 375px mobile width.
  </action>
  <what-built>
    Complete kitchen display page at `/kitchen/[token]` with:
    - Dark navy background with Montserrat font
    - Two count cards (veg emerald green, non-veg brand orange) showing estimated or confirmed counts
    - Dish cards with photos, names (32px+), and veg/non-veg badges
    - Full-screen recipe modal with sticky Ingredients / Method / Notes tabs
    - 60-second auto-refresh with "Last updated HH:MM" timestamp
  </what-built>
  <how-to-verify>
    1. Open the screenshots in /tmp/kitchen-verification/ — verify dark background, readable text, no layout breakage at 375px
    2. Open `http://localhost:3000/kitchen/[dev-token]` in your browser
    3. Use Chrome DevTools responsive mode at 375px width (iPhone SE)
    4. Check: Both count cards fit side-by-side without wrapping
    5. Check: Dish photos load, names are large (32px+), badges are colored correctly
    6. Tap a dish card — recipe modal should open full-screen
    7. Switch between Ingredients / Method / Notes tabs — content should change, tabs should stick
    8. Scroll within the modal — content scrolls, header + tabs stay fixed
    9. Close modal via X button — returns to dish list
    10. Wait 60 seconds — page should refresh (check "Last updated" timestamp changes)
    11. Try an invalid token URL — should show 404 page
  </how-to-verify>
  <verify>User types "approved" or describes issues</verify>
  <acceptance_criteria>
    - User confirms dark background with no white flash
    - User confirms count cards fit side-by-side at 375px
    - User confirms dish photos load and names are large
    - User confirms recipe modal opens, tabs work, scrolling works
    - User confirms 404 on invalid token
  </acceptance_criteria>
  <done>User approves the kitchen display visual design and functionality at 375px</done>
  <resume-signal>Type "approved" to complete Phase 3, or describe any layout/visual/functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
User confirms the kitchen display works correctly at 375px on a phone-sized screen.
</verification>

<success_criteria>
User approves the visual design, layout, and functionality of the kitchen display at 375px.
</success_criteria>

<output>
After completion, create `.planning/phases/03-kitchen-display/03-03-SUMMARY.md`
</output>
