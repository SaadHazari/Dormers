import { BotMessageSquare, type LucideProps } from 'lucide-react'

// Doro's avatar: an assistant that talks. One place to change it, so every
// Doro tile, chat header and CTA stays in step.
export function DoroMark(props: LucideProps) {
  return <BotMessageSquare {...props} />
}
