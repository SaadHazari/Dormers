// Compatibility shim — moved to @/contexts/chatbot/ui/chat-bus in Phase 4.
// The bus is a UI concern (window events for open/close) so it lives in
// chatbot's ui/ ring rather than domain/.
export * from '@/contexts/chatbot/ui/chat-bus'
